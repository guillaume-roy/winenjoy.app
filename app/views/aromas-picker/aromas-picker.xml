<Page xmlns="http://schemas.nativescript.org/tns.xsd"
        shownModally="onShownModally">
    <GridLayout rows="auto, *, auto">
        <SearchBar hint="{{ isDefects ? 'Rechercher un défaut' : 'Rechercher un arôme'}}" id="searchBar" textFieldHintColor="#FFFFFF" />
        <ListView items="{{ aromaCriterias }}" itemTap="onSelectAroma" row="1">
            <ListView.itemTemplate>
                <GridLayout class="list-item">
                    <Label text="{{ aroma.label }}" />
                    <Label text="✓" visibility="{{ isSelected ? 'visible' : 'collapsed' }}" class="check-icon" />
                </GridLayout>
            </ListView.itemTemplate>
        </ListView>
        <Button text="Valider" row="2" tap="onValidate" class="validate-button" />
    </GridLayout>
</Page>